# BUILD-23 — Cross-surface consistency report

- **Org:** CREO Arts
- **Source:** https://nonprofit-erp-production.up.railway.app
- **Run:** 2026-07-19T07:23:29.639Z
- **Scope:** donors 16 · gifts 38 · ledger rows 27 · interactions 37
- **Result:** 0 ERROR · 2 WARN · 5 INFO · 11 OK

This is the backward sweep (BUILD-23 Part 1): a read-only reconciliation of one
org's live data against the canonical consistency invariants. Generated by
`scripts/consistency-audit.js`. **ERROR** = a correctness defect with a clear
root cause; **WARN** = a reconciliation gap to eyeball (usually a seed/manual
artifact); **INFO** = expected-by-design divergence; **OK** = invariant holds.

## Remediation log (the BUILD-23 record)

The first run of this audit against CREO found **1 ERROR** — the exact
duplication class BUILD-23 exists to catch — plus a $50k fund-reconciliation
gap that turned out to be the same root cause:

| Finding (first run) | Root cause | Action |
|---|---|---|
| `manual-gift-twin` — 2 legacy manual "Gift from …" ledger rows (`ft_4ef9d3b3` $50k Margaret Chen, `ft_b590af4d` $10k Owen Bishop) duplicated a `source=gift` twin | The pre-BUILD-21 donor-profile double-log: one gift stamped the ledger **twice** (once `source=gift`, once `source=manual`). The product code paths are now single-stamp (proven by `tests/finance-gift-stamp.test.js` 17/17); these were **pre-existing data artifacts** on the demo org, not a live regression. | **Fixed** — `node scripts/dedupe-finance-gift-stamps.js --apply` (dry-run confirmed first, 2 rows exported to `docs/finance-gift-stamp-dupes-removed-2026-07-19.json` before delete, removed via the audited admin DELETE API). |
| `fund-balance-total` — Σ(fund balances) $86,339 ≠ Cash on Hand $136,339 ($50k unassigned) | The Margaret Chen manual twin was a $50k income row **with no fund designation** — so it counted toward Cash on Hand but not any fund. | **Fixed by the same cleanup** — removing the twin made Σ(fund balances) $76,339 == Cash on Hand $76,339 exactly. |

**Result after remediation: 0 ERROR.** Cash on Hand corrected $136,339 → $76,339
(the $60k of doubled gift income removed). The two remaining WARNs and all INFOs
below are **hand-authored seed-data artifacts on the demo org, not code defects**
(see each entry). The critical duplication class — one gift → one ledger stamp —
now holds cleanly, and the `tests/consistency-e2e.test.js` guardrail (Part 2)
prevents it from ever regressing silently.

### Remaining WARNs — why they're left (not code bugs)

- **donor-lifetime · Sunrise Foundation** — 6 historical grant gift rows sum to
  $135,000 but the donor's stored `total_giving`/`gift_count` were hand-seeded to
  $75,000/3. This is inconsistent **seed authoring**, not a code path — the
  product's `recalcDonorSummary` (fired on every gift add/edit/delete) would
  reconcile it; it's left untouched rather than mutate demo gift rows on prod.
- **period-finance-gift** — a **$136** residual between gift-sourced FY ledger
  income ($70,137) and Reports FY gift total ($70,001), i.e. 0.19%. Seed rounding
  / a boundary-dated stamp; immaterial and demo-only. (Before cleanup this gap was
  $65,002 — the $60k of twins + this residual.)

## Warnings — reconcile / eyeball (2)

- **donor-lifetime** — 1 donor(s): total_giving ≠ Σ(gift rows) and NOT explained by import
  - Sunrise Foundation: total $75,000 vs Σgifts $135,000 (6 rows)
- **period-finance-gift** — Gift-sourced FY ledger income $70,137 ≠ Reports FY gift total $70,001 — some FY gifts aren't stamped to the ledger (or vice-versa)
  - Non-gift FY ledger income (grants/manual, legitimate): $5,002. Finance FY revenue $75,139 = gift $70,137 + non-gift $5,002.

## Info — expected by design (5)

- **gift-no-stamp** — 37/38 gift row(s) have no fin_transaction stamp (typical of imported/seed gifts)
  - Not a defect: gift_id stamping is enforced going forward; historical rows predate it.
- **ledger-null-giftid** — 2 non-manual ledger row(s) have a null gift_id (legacy, pre gift_id stamping)
  - ft_d9daa105 [gift], ft_0ad00f41 [gift]
- **gift-interaction** — 11 donor(s) have a gift-count ≠ gift-interaction-count (expected for imported/seed gifts)
- **donor-lifetime-import** — 4 donor(s) carry imported lifetime giving with fewer/no gift rows (by design — Top Donors reads total_giving)
- **goal-rollup** — No typed roll-up goal portfolio configured (single-goal hero only)

## OK — invariants that hold (11)

- **gift-double-stamp** — Every stamped gift has exactly one fin_transaction (no double-stamp)
- **manual-gift-twin** — No legacy manual gift-twin ledger rows
- **cash-on-hand** — Cash on Hand $76,339 == Σ(ledger) $76,339
- **fund-balance-perfund** — Every fund's reported balance == Σ(its ledger transactions)
- **fund-balance-total** — Σ(fund balances) $76,339 == Cash on Hand $76,339
- **period-reports-fundraising** — Reports FY gift total $70,001 == Fundraising this-period raised $70,001
- **goal-nonneg** — Active goal "Raise $25,000 this quarter": $75,501 of $25,000 (100%)
- **orphan-ledger-donor** — No ledger row references a missing donor
- **orphan-gift-donor** — Every gift references a live donor
- **home-pipeline-value** — Home pipeline value $222,251 == Σ(donor lifetime giving) $222,251
- **top-donors-total** — Top-Donors (lifetime) Σ $222,251 == Σ(donor lifetime giving) $222,251

---
_Re-run: `node scripts/consistency-audit.js`. The forward guardrail against this
whole class is `tests/consistency-e2e.test.js` (BUILD-23 Part 2)._
