# Architecture

Read this when you touch the stack, env vars, project layout, `server.js`/`db.js`/`routes/`/`shared/`, the schema, org scoping, the actor stamp, API keys, assets, export or scale.

## Rules
- **Code that both client and server run lives in `shared/` as ESM, never in `client/src/lib/`.**
  `.railwayignore` strips `client/` from the backend artifact, and `shared/package.json` declares
  `"type":"module"`. (BUILD-79)
- **The server reaches a `shared/` module only through dynamic `import()`.** It caches the module
  in a promise or loader function beside the first use, because server.js is CommonJS. (BUILD-79)
- **A `shared/` module the client imports must not import root CommonJS such as `orgTime.js`,
  because Rollup refuses it.** Mirror only the fields the client needs, and pin that mirror to the
  real registry with a Node test. Settings.jsx's `DIRECT_ORDER` is the example. (BUILD-91)
- **`git add` a new root module before pushing.** `deploy-shape` builds the artifact from
  `git ls-files` minus `.railwayignore`, so an untracked file becomes `ERR_MODULE_NOT_FOUND` in
  prod. (BUILD-84, BUILD-90, BUILD-94)
- **Require a directory's `index.js` by its full path (`./sources/index.js`).** A bare directory
  require escapes the artifact check. (BUILD-89S)
- **Write schema order as a fresh database runs it.** `initSchema` in db.js runs top to bottom,
  so a new table goes after every table it references. An upgraded database hides the mistake,
  and CI's "Boot the API server" step, which gates both deploys, catches it. (BUILD-88c)
- **Follow `ADD COLUMN IF NOT EXISTS … DEFAULT x` with `ALTER COLUMN … SET DEFAULT x`.** The
  first statement does nothing on a database that already has the column, default included.
  (BUILD-94)
- **Schema failure is fatal at boot, and seed failure is not.** `getDb()` catches `seedData()`.
  Keep seed row ids date-stable, so that an id collision always implies an `ON CONFLICT` match.
  (Demo-seed fix)
- **Scope every read and write by `org_id`, and pass any foreign id from a body or param through
  `orgOwns(table, id, orgId)`.** A foreign id answers 404, never 403, with a body identical to a
  missing row's. (BUILD-13, BUILD-75)
- **Resolve ownership before a feature gate.** A cross-tenant probe must get 404 and not the 503 of
  `aiGate` or a similar gate. Order on a plan-gated write is `requirePlan`, then
  `checkWriteAccess`, so Core gets 403 and a lapsed org gets 402. (BUILD-45, BUILD-99)
- **Every mutating route carries `checkWriteAccess`, and reads never do.** Deletes and every data
  export are also never write-gated, because a lapsed org must always be able to leave with its
  data. (BUILD-03, BUILD-45)
- **`GET /org/export/full` finds every `org_id` table through `information_schema`, so a new
  table exports the day it exists.** Name a new secret column so `EXPORT_WITHHELD_COLUMN` catches
  it (password, token, secret, sealed, hash, api_key, credential, pdf_data). (BUILD-98 switch)
- **Keep the read-only API as two doors with two locks.** `requireApiKey` guards `/api/v1/*`
  only, and `requireAuth` never accepts a key. The org comes from the key's stored row, never from
  a header or query. A revoked key answers the same 401 as an unknown one. (BUILD-98 switch)
- **Store a credential only sealed by `shared/secretBox.js` with AAD bound to the org.** There is
  no plaintext fallback: with no key the route answers 503 and writes nothing. (BUILD-89S,
  BUILD-98 switch)
- **A new insert into an actor table stamps `created_by`/`created_by_name` or is listed in
  `CLASSIFIED` in `tests/actor-stamp.test.js`.** A new actor table joins `ACTOR_TABLES`. (BUILD-75)
- **Only `destroyAsset()`, in assetStore.js's `DESTRUCTION SEAM` block, removes asset bytes.**
  Prune is a 90-day soft delete, and ids are kind-salted by `assetIdFor`. A new pointer site logs
  to `asset_pointer_history` and is classified in `asset-retention`. (BUILD-56)
- **Org deletion does not yet touch the asset tables.** Fold them into the cascade when the
  org-deletion build happens. Remove a test org with `DELETE /admin/orgs/:id`, never by hand.
  (BUILD-56)
- **Design for 25k donors, 200k gifts and 150k interactions per org.** A per-donor aggregate is
  one `LEFT JOIN … GROUP BY`, never a correlated subquery, and a new per-donor lookup gets its
  index in `initSchema`. (BUILD-05)
- **A list route paginates with `limit`/`offset` and a whitelisted sort that has an `, id`
  tiebreak.** `GET /donors` keeps the legacy unpaginated array when `limit` is absent. The Home
  stack reads `/donors/summaries`. (BUILD-06)
- **Declare a literal route before its `/:id` sibling** (`/donors/summaries`,
  `/custom-fields/reorder`), because Express matches in order. (BUILD-06)
- **Mount `compression()` on the `/donors` family only**, never app-wide, so that SSE
  (`/ai/stream`) and webhooks are never buffered. (BUILD-06)
- **Keep the 30mb body parser on the import routes only.** Every other route keeps the global
  5mb cap. (BUILD-05)
- **When a unique index is impossible, serialise same-key creates with `withAdvisoryLock(key, fn)`
  from db.js.** Examples are `donor:<org>:<email>` and `import:<org>`. (BUILD-27)
- **Never set a test-only seam in production** (`TEST_MODE`, `DISABLE_RATE_LIMIT`, `*_API_BASE`,
  `RESEND_BASE_URL`, `DISABLE_BACKGROUND_TICKS`, `SESSION_CACHE_TTL_MS=0`). A test-only route checks
  `testMode()`, a function, and 404s otherwise. (BUILD-45, BUILD-84, BUILD-90)

## Gotchas
- **Use a regex route (`app.all(/^\/api\/v1.../)`) and the route inventory breaks**, because it
  splits string paths. Let the final 404 handler answer unknown paths. (BUILD-98 switch)
- **A variable filled inside `withTransaction` and read after it must be declared outside it.**
  Otherwise a ReferenceError returns a 500. (BUILD-98 switch)
- **A `//` inside a SQL template literal is SQL, and a backtick ends the literal.** Put comments
  above the `await run(` call or use `--`. Placeholders in `INSERT … SELECT ?,?` need casts.
  (BUILD-94, BUILD-99)
- **A server-side cache that keys on org identity must honour `SESSION_CACHE_TTL_MS=0`.** `orgTz`
  once served a deleted org's state to the next suite run. (BUILD-84)
- **`archiver` v8 exports a class (`new ZipArchive({...})`), not the old factory.** Set response
  headers only after every query succeeds, so that a DB error still returns a clean JSON 500.
  (BUILD-03)
- **Only `routes/migc.js` is split out.** Every other route lives in server.js (~38k lines), so
  grep there first. (MIGC)

## Where the code is
- `server.js` — Express app, nearly every route, `orgOwns`, `checkWriteAccess`, `requireApiKey`
- `db.js` — pg pool (SSL hardcoded), `initSchema` (order matters), `seedData`, `withAdvisoryLock`
- `auth.js` — `requireAuth`, `requireAdmin` (and the MFA setup-only session)
- `shared/` — pure ESM modules for client, server and suites (`shared/package.json` marks ESM)
- `assetStore.js` — asset put/prune/purge and the one `destroyAsset` seam; `scripts/restore-asset.js`
- `stripeKeys.js` — the two independent Stripe clients (donation vs platform billing)
- `routes/migc.js` — the only split-out router (Mi Gulf Coast)
- `GET /org/export/full`, `GET /org/export/csv`, `GET /donors/export/csv` — the data-export family
- `tests/deploy-shape.test.js` — the artifact and ESM-marker check behind the `shared/` rules

---

The sections below were moved verbatim from the old CLAUDE.md. Build entries they cite live in `docs/HISTORY.md`.

## Strategic pivot (2026-07-12) — READ THIS FIRST
Steward pivoted from a full 11-tab nonprofit ERP to a focused **retention/stewardship product** built around Donors + Grants + Communications.

- **What's hidden, not deleted**: Events, Volunteers, Board, Tasks, and Finance are commented out of the nav (`TABS`/`BOTTOM_TABS`/`MORE_TABS` in App.jsx — see "Active tabs" below for the exact current arrays). Their component files, backend routes, and database tables are all still fully intact and functional — this is a UI-visibility change only, deliberately reversible by uncommenting.
- **Analytics is dead, not paused (deleted 2026-07-16, BUILD-04 Strike 2)**: `Analytics.jsx` and the zero-caller `GET /analytics` route were deleted outright (along with the long-unrouted `TodayPage.jsx`). Analytics computed its 7 charts client-side from the shared `data` prop plus `/campaigns` + `/events` — both of those routes have other callers and remain. If analytics is ever wanted again it gets rebuilt against the retention product, not restored.
- **A donor-facing portal was explored and rejected.** A partner brief proposed a donor-facing "Lifetime Impact Dashboard" — donor login, giving tiers, badges, milestone displays. Rejected specifically because nonprofit-experienced feedback identified gamified donor-facing portals (tiers/badges/leaderboards visible to the donor) as damaging to donor trust, not building it. This is why donor login, tiers, and badges were deliberately avoided in what got built instead — see below.
- **What got built in its place**: a staff-facing retention/stewardship engine. The system notices patterns in donor data (giving milestones, anniversaries, contact gaps) and drafts or suggests — a human always reviews and sends. Nothing donor-facing was built; there is no donor login.
- **AI works invisibly, not as a branded feature.** The general-purpose "Ask AI" chatbot (a floating assistant you could ask anything) was **removed entirely** — not hidden, actually deleted (`AIChat`, formerly exported from Dashboard.jsx — see "Component files" below) — because a visible, brandable "AI feature" cuts against the product's actual value: staff shouldn't need to think about *which* moments are AI-touched, they should just see a good suggestion at the right time. Every specific AI-powered action (milestone drafting, LOI drafting, forecast, wealth scoring, etc.) still works exactly as before — only the standalone chat surface is gone. See "Key patterns" for the current de-branded label conventions (`AIBtn`'s default label is "Suggest", not "AI Assist"; `AIPanel`'s badge reads "Suggested", not "AI Intelligence").

## Stack
- Frontend: React 18 + Vite → deployed on Vercel
- Backend: Node + Express → deployed on Railway
- Database: Supabase PostgreSQL
- Auth: JWT written directly to localStorage (npe_token, npe_user, npe_org)
- AI: Anthropic SDK (claude-sonnet-4-6) — used for specific, narrow tasks throughout the app; there is no general-purpose chat interface (see "Strategic pivot" above)
- Email: Resend HTTP API (noreply@stewardapp.dev)
- Payments: Stripe Connect Express

## Live URLs
- Frontend: https://client-five-tau-13.vercel.app (also stewardapp.dev via Vercel nameservers)
- Backend: https://nonprofit-erp-production.up.railway.app
- GitHub: github.com/jonnycodes420/nonprofit-erp
- Demo login: admin@creoarts.org / demo1234 (org_creo — staff-side org name is **"CREO Arts (Demo)"** since 2026-08-12; donor-facing surfaces read `portal_settings.display_name` = "CREO Arts" and are unaffected)
- **org_creo is DEMO-ONLY and carries FABRICATED legal/tax identity** (invented legal name "CREO Arts Collective, Inc.", an invented NYC receipt address, invented ED signature "Maya Reyes" — set 2026-08-12 so demo receipts could be issued). **When the real CREO Arts onboards, it starts as a FRESH org record — NEVER by converting/renaming org_creo** — the fabricated identity must never appear on a real organization's receipts, and org_creo's donors/gifts/receipts are demo fiction end to end.
- Demo DONOR (portal): xjca2006+demo@gmail.com = Renee Castillo (`dseed_03`, $2,500 lifetime, quiet mid-level) in org_creo — request a magic link at https://www.stewardapp.dev/portal/creo-arts-creo (set up 2026-08-11; proof shots in docs/demo-donor-2026-08-11/)

## Required Environment Variables
### Backend (Railway)
- `ANTHROPIC_API_KEY` — claude-sonnet-4-6
- `SUPABASE_URL` — Supabase project URL
- `SUPABASE_KEY` — Supabase service role key
- `JWT_SECRET` — JWT signing secret
- `RESEND_API_KEY` — Resend API key for email campaigns
- `STRIPE_SECRET_KEY` — Stripe secret key used for **DONATION processing** (connected-account charges + `/stripe/webhook`). In production this is a **LIVE** key.
- `STRIPE_BILLING_SECRET_KEY` — optional; Stripe secret key used for **PLATFORM subscription billing** only (`create-checkout`/`create-portal`, the platform Stripe customer, `/billing/webhook`). **Falls back to `STRIPE_SECRET_KEY` when unset** (so production needs no new var). Set this to a **test** key (`sk_test_…`) to run billing in Stripe test mode while donations stay on the live `STRIPE_SECRET_KEY` — the two Stripe clients (`stripe` vs `billingStripe`) are independent and never cross-wired (resolver: `stripeKeys.js`). Provision test prices + the test billing webhook together with the same test key.
- `STRIPE_WEBHOOK_SECRET` — Stripe webhook signing secret (Connect/donation events at /stripe/webhook)
- `STRIPE_BILLING_WEBHOOK_SECRET` — Stripe webhook signing secret for /billing/webhook (platform subscription events; can reuse STRIPE_WEBHOOK_SECRET if same endpoint)
- `FOUNDER_EMAIL` — from address for onboarding email sequence (e.g. jonathan@stewardapp.dev); must be a verified Resend sender
- `STRIPE_PRICE_CORE` — Stripe Price ID for the **$249/mo** Core plan (BUILD-90 prices; BUILD-24 shipped a lower set)
- `STRIPE_PRICE_TEAM` — Stripe Price ID for the **$499/mo** Team plan
- `STRIPE_PRICE_FOUNDING` — Stripe Price ID for the **$199/mo** founding-partner price (Core tier; super-admin-assigned, and one of the three a close link may sell). Provision all three with `scripts/create-billing-products.js` (test mode first). **`closeLink.js`'s `CLOSE_PLANS` is the source of truth for the amounts** and `tests/one-date.test.js` checks the provisioning script, the MRR table and the pricing page against it. **A close link REFUSES to mint a Checkout session for a plan with no price id set** — that is deliberate: a link that cannot charge is better than one that silently charges the wrong amount
- `STRIPE_BILLING_API_BASE` — **LOCAL-TEST SEAM ONLY** (BUILD-90), the platform-billing twin of `STRIPE_API_BASE`: when set, `billingStripe` talks to a local mock so the close-link path is drivable with no credentials and no network. Never set in production
- `STRIPE_PRICE_SEED`/`STRIPE_PRICE_GROWTH`/`STRIPE_PRICE_IMPACT` — LEGACY $99/$249/$499 Seed/Growth/Impact price IDs. Superseded by Core/Team (BUILD-24) but still recognized by `create-checkout` so a pre-cutover org can reactivate on its old price; safe to leave unset once no org is on them
- `FRONTEND_URL` — used in invite links and Stripe redirects
- `SENTRY_DSN` — backend error reporting; also gates `Sentry.setupExpressErrorHandler` and the process-level `uncaughtException`/`unhandledRejection` handlers (see Error monitoring below) — unset means none of that runs, not a hard failure
- `STEWARD_CREDENTIAL_KEY` — **required before any organisation can connect a giving source** (BUILD-89S 89a). At least 32 characters (`openssl rand -base64 32`); seals provider API keys at rest with AES-256-GCM bound to the org id. **There is no fallback and no plaintext path**: unset, `seal()` throws, the connect route answers 503 and nothing is written, so the feature is unavailable rather than insecure. `GET /giving-sources/providers` reports `credentialsReady`. **Rotating it orphans every stored credential** — every source must be disconnected and connected again (no re-seal path yet; `BLOCKED-build89a.md`).
- `RECOVERY_SECRET` — optional; signs the donor card-update link token (see "Recurring gift recovery"). Falls back to `JWT_SECRET` if unset — not a hard requirement to add, just lets that token family be rotated independently later

### Frontend (Vercel / client/vercel.json)
- `VITE_API_URL` — Railway backend URL
- `VITE_SENTRY_DSN` — client error reporting (build-time; baked into the bundle, not a runtime secret)
- `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` — build-time only, used by `@sentry/vite-plugin` to upload source maps during `vite build` so production stack traces resolve to real file/line instead of minified output; the plugin no-ops (via `disable: !process.env.SENTRY_AUTH_TOKEN`) when `SENTRY_AUTH_TOKEN` isn't set, so local/preview builds without the secret still succeed. `SENTRY_AUTH_TOKEN` is a Sentry **org auth token** (from sentry.io → org settings → Auth Tokens), not the DSN — treat it like any other build secret (Vercel project env var, not committed)

## Error monitoring
- Backend: `Sentry.init()` gated on `SENTRY_DSN` (server.js top). `Sentry.setupExpressErrorHandler(app)` catches errors that flow through Express's request/response cycle. `process.on("uncaughtException")` and `process.on("unhandledRejection")` (registered immediately after `Sentry.init()`, before any other require) are the backstop for everything Express never sees — background jobs (`syncAllGmail`, `processSequences`, `checkTrialExpiry`, etc.), truly synchronous throws, or a rejected promise nobody `.catch()`ed. `uncaughtException` reports to Sentry then exits (`Sentry.close()` → `process.exit(1)`) since Node's own guidance is not to keep serving requests after one — Railway restarts the process clean. `unhandledRejection` reports and deliberately does NOT exit, since in this codebase it's overwhelmingly a rejected promise inside a fire-and-forget background job (most of which already have their own local `.catch(console.error)`); killing the whole API over one of those would be worse than the bug itself.
- Client: `Sentry.init()` gated on `VITE_SENTRY_DSN` (main.jsx top), with `browserTracingIntegration()`. Source maps: `vite.config.js` sets `build.sourcemap: !!process.env.SENTRY_AUTH_TOKEN` (not unconditionally `true` — a build with no token generates no maps at all, so there's never a window where maps sit in `dist` unuploaded-and-unstripped) and runs `@sentry/vite-plugin`'s `sentryVitePlugin(...)`, which uploads maps to Sentry at build time then deletes them from the `dist` output (`sourcemaps.filesToDeleteAfterUpload`) regardless of whether the upload itself succeeded — production never publicly serves raw source maps either way.
- **Production status (verified 2026-07-16, BUILD-04 Strike 4)**: **Backend IS live** — `GET /health` now reports `sentry: true/false` (non-secret boolean; Railway returned `true`), and `POST /admin/debug/sentry-test?mode=route|rejection` (requireAuth + requireAdmin) fires one deliberate error down the Express-error-handler or `unhandledRejection` path respectively — both paths exercised against production (route → 500, rejection → 200 with the process staying up, as designed). Event *arrival in the Sentry dashboard* still needs a human check — no Sentry credentials in the dev environment. **Client is NOT live** — the deployed Vercel bundle contains no DSN string and no `_sentryDebugId` markers, meaning `VITE_SENTRY_DSN` and `SENTRY_AUTH_TOKEN` are both missing from Vercel's build env; browser errors currently report nowhere and prod stack traces would be minified. Fix: add both in Vercel project env vars (plus `SENTRY_ORG`/`SENTRY_PROJECT`) and redeploy.

## Uptime monitoring (decision — external ping, no code)
Decided 2026-07-16 (BUILD-04 Strike 4): use a free external ping service rather than building anything — Sentry catches errors *inside* a running process; only an outside observer catches "Railway/Vercel is down entirely."
- **What to monitor**: `GET https://nonprofit-erp-production.up.railway.app/health` (expect 200 + `"status":"ok"` — also carries `db` and `sentry` booleans) and `https://www.stewardapp.dev/give/creo-arts-creo` (frontend + the revenue-critical public donate path — slug verified live 2026-07-16; use the `www.` host, the apex 307s to it).
- **Setup (user click-through, ~5 min)**: create a free UptimeRobot (50 monitors, 5-min interval) or Better Stack (10 monitors) account → add the two HTTP(S) monitors above → set keyword check `"status":"ok"` on the health monitor → alert contact = founder email (jonathan@stewardapp.dev). Not automatable from this environment (needs an account signup).

## Project structure
- /client/src/App.jsx — AppShell + TABS + App, imports from components/. **Desktop shell is a fixed 220px left sidebar (2026-07-17 restructure)**: dark green `#0f1a12`, wordmark at top (no icon-in-square logo — icon lives on the favicon only), main nav items with icons, Settings pinned at bottom; active item = gold left accent on `bgElevated`. **Sidebar is pure nav as of BUILD-08 (2026-07-17)** — the user chip/sign-out moved to the global top bar (below). Content lives in an `.app-main` column (`marginLeft:220` + `marginTop:52`). **Global top bar — full-width (BUILD-10, 2026-07-17)**: slim 52px `.app-topbar` (`components/TopBar.jsx`) spanning the FULL viewport width (`position:fixed`, top:0/left:0/right:0, `zIndex:250`) ABOVE the sidebar — which now starts at `top:52` (`height:calc(100vh−52px)` via top:52/bottom:0) and is pure nav (Settings pinned bottom). Ink `#0f1a12`, hairline `bgElevated` bottom border. Left→right: the **Steward wordmark** (moved out of the sidebar into the bar's left edge — a 220px-wide button sitting over the rail zone with the same 20px inset, links to Home via `onNavigate("dashboard")`), global search (cap ~560px via `flex:"0 1 560px"`, "Search donors, grants… ⌘K"), spacer, "?" help menu, user chip/sign-out. ⌘K/Ctrl+K focuses the search from anywhere via a window keydown listener; **it now works everywhere including over the z-200 full-screen takeovers** — the bar (z 250) sits above them so nothing occludes it, and the old `elementFromPoint` no-op guard was removed (only the hidden-on-mobile width/height check remains). The two takeovers (DonorProfile/GrantProfile) carry a `.fullscreen-takeover` class and render at `top:52` on desktop (below the bar, back button fully visible); a `.fullscreen-takeover{top:0!important}` rule in GlobalStyles' 768px block keeps them full-screen on mobile. Verify **no double top-offset** — the bar is `fixed` (out of flow), so `.app-main`'s single `marginTop:52` is the only offset. V1 search scope: donors (name/email via `GET /donors?search=&limit=5`), grants (funder/program via `GET /grants?search=&limit=5` — params added for this, no-param behavior unchanged), plus static quick-nav actions ("Reports", "Settings → Tax Receipts"); 220ms debounce, org-scoped server queries only, grouped dropdown (Donors/Grants/Go to) with arrow-key/Enter navigation; results deep-link through `navigateTo` (donor → `selectDonorId`, grant → `grantId`). Right side: "?" help menu (mailto jonathan@stewardapp.dev — no docs site exists yet) + user chip/sign-out. The dark-bar input needs its own gold focus rule (`.topbar-search:focus` in GlobalStyles) because the global `input:focus` rule is `!important` green-on-cream. **navNonce remount pattern**: Donors/Grants/Communications/Settings consume their `initial*` intent props on mount only, so `navigateTo` bumps a `navNonce` — used as those components' React key — whenever it carries an intent; this makes same-tab deep-links work (e.g. searching a grant while already on Grants). Plain nav (no opts) never remounts. The old sticky header still exists but is **mobile-only** (`display:none` inline; GlobalStyles' 768px block restores it with `!important`) — mobile ≤768px is deliberately unchanged: sidebar + top bar hidden (`.app-topbar{display:none!important}`), bottom bar + More drawer exactly as before, no ⌘K. Sidebar buttons route through `navigateTo` so cross-tab intents (comms/donors/grants/settings) clear on manual nav. No "Ask AI" button (removed, see Strategic pivot). **Per-tab width strategy (BUILD-06 Phase E, 2026-07-17)**: the `app-content` wrapper gives Home a centered ~1200px column (reading page) while every workspace tab (Donors/Grants/Communications/Reports/Settings + hidden legacy tabs) is fluid full-width with 32px side padding; the shell background matches the tab (`T.bgDeep` on Home so Dashboard's `dash-bleed` never leaves lighter gutters, `T.bg` elsewhere). Mobile is exempt — GlobalStyles' 768px `!important` rules override the inline widths. Grants Kanban columns are `flex:"1 1 260px"` (grow to fill wide screens). (The Donor Kanban / My Pipeline board this note once also covered was retired in BUILD-19 — the Pipeline tab is now the one board.)
- **Section-level nav is horizontal top tabs everywhere (2026-07-17)**: `SectionTabs` in shared.jsx (underline tabs, gold active indicator, optional icon/badge, scrolls horizontally when it doesn't fit — no media query needed) renders Communications' six subtabs (`.comm-tabbar`), Reports' six-report picker (`.reports-tabbar`, with the active report's question as a subtitle line below), and Settings' eight section groups (`.settings-tabbar`). Settings sections are deep-linkable: `navigateTo("settings",{section:id})` → `settingsIntent` → Settings' `initialSection` prop (ids: org/team/integrations/giving/customization/receipts/data/account); Communications' CAN-SPAM prompt uses this to land directly on `receipts`.
- scripts/screenshot-matrix.js — committed layout verification: 390/768/1440/1920/2560 × all-tab screenshots (home, donors-directory, donors-kanban, grants, communications, reports, **finance** — added BUILD-10, the owed BUILD-09 mobile check — settings), shell assertions (sidebar + top bar vs bottom-bar visibility, no horizontal page scroll), and deep-link smoke checks (Communications tabs, LYBUNT row → donor profile, Settings receipts tab). Playwright is deliberately NOT a project dep — install it in a scratch dir and pass `PLAYWRIGHT_DIR`; run against a local `vite preview` won't work with the prod API (CORS allowlist), so point `BASE` at the deployed frontend. Latest run: docs/layout-matrix-2026-07-17-fullwidth/ (14 passed / 0 failed / 1 data-dependent skip, against prod, BUILD-10 full-width bar, incl. mobile+desktop Finance).
- scripts/topbar-verify.js — committed functional verification for the global top bar (same env conventions as screenshot-matrix.js): ⌘K focus from three tabs, donor search → correct profile, grant search cross-tab AND same-tab (the navNonce case), quick-nav to Reports + Settings→Tax Receipts, help menu. **BUILD-10 geometry assertions**: exactly one bar + `position:fixed`, bar 52px tall spanning the full viewport (x=0, width≥vw), wordmark in the bar not the sidebar, sidebar starts at y=52, content single 52px offset (no double-offset), full-screen takeover renders below the bar (y≈52) with the bar still visible, ⌘K focuses search even while a takeover is open. Latest run: 23/23 against prod (2026-07-17, BUILD-10).
- /client/src/main.jsx — router, auth context, route guards — ALL ROUTING LIVES HERE ONLY. `/today` still redirects to `/dashboard` (kept for bookmarks) — `TodayPage.jsx` itself was deleted 2026-07-16 (BUILD-04 Strike 2). **Route-split as of BUILD-07 (2026-07-17)**: every route except Landing is `React.lazy` behind one Suspense fallback (cream + spinner) — visiting `/` loads a 201KB entry chunk instead of the whole ~1.5MB app; App/Donors/Admin/etc. fetch when their routes mount. Landing stays an eager import deliberately.
- **scripts/landing-capture.js — DELETED (BUILD-12).** The raster capture pipeline (and its `lp-*.webp` assets + `docs/landing-capture-manifest.json`) is retired: landing product shots are now LIVE DOM/SVG, not screenshots (see Landing.jsx bullet). Do NOT reintroduce a raster capture step for these — vector text is crisp by construction, and three raster "fixes" (BUILD-08 → BUILD-10 Part 2 → BUILD-11) proved a downscaled bitmap of antialiased UI text can't be made crisp at every DPR.
- scripts/landing-crispness-prod.js — committed crispness gate (BUILD-12), **runs against the DEPLOYED page, not committed files** (the whole BUILD-12 lesson: the old check passed 12/12 twice against the WebP files while retina still looked blurry — it measured the wrong bytes). Loads prod at **DPR 2 and 3** and, per product visual (hero goal + retention, queue, receipt, recovery email, import, climb, calculator — keyed to stable class hooks `.lp-goalcard`/`.lp-retcard`/`.lp-qcard`/`.lp-receipt`/`.lp-email`/`.lp-import`/`.lp-climb`/`.lp-calc-card`): asserts it is present and is **live DOM, not a raster `<img>`**, and that there is **zero** `/lp-` product `<img>` anywhere (any such img is a regression to the blur). Keeps a raster-fallback branch (if a product img ever survives: fetched `currentSrc` bytes must decode to `naturalWidth ≥ rendered×DPR` and be a lossless/high-quality type — the check the old script should have done against served bytes). Saves DPR-3 crops of hero/receipt/queue to `OUT` for the human eyeball gate. Same PLAYWRIGHT_DIR/BASE conventions. Latest run: **34/34 against PROD** (https://www.stewardapp.dev, post-deploy) — committed DPR-3 prod crops in docs/landing-crispness-2026-07-18/ are visibly sharp (the human gate). Also 34/34 on local preview (docs/landing-crispness-local/).
- scripts/landing-image-verify.js — **DOM-aware rewrite (BUILD-12).** Its old premise (every product `<img>` supplied ≥N× raw pixels) is obsolete now that the shots are DOM. It now asserts, at DPR 2 and 3, that every product visual is present **as DOM** (not a raster `<img>`) and that there is no `/lp-` product image. The richer byte-level/crop-saving check is `landing-crispness-prod.js` above. Latest run: 18/18 (local preview of the BUILD-12 build).
- scripts/landing-funnel-verify.js — committed honesty-gate + funnel check (BUILD-11 Build B). Asserts: the funnel sections render **in order** (hero → wedge calculator → problem → 3 product moments → how-it-works → money strip → candor → founder → close), the interactive recurring-loss **calculator computes correctly** for multiple slider inputs (`monthly × 12 × 0.24`, read from `.lp-calc-loss`) and states its assumption, **no fabricated social proof** (grep guard: no "trusted by"/"as seen in"/star ratings/invented user counts/testimonial-quote-with-attribution/logo-bar images — deliberately NOT a bare `/testimonial/` since the candor section honestly says it won't "sell you a testimonial"), and every CTA links live (Start free → /signup, real footer links). `innerText` is compared **case-insensitively** because CSS `text-transform:uppercase` on the eyebrows changes what `innerText` returns. Same PLAYWRIGHT_DIR/BASE conventions as the other landing scripts. Latest run: **24/24** against a local `vite preview` of the BUILD-12 landing (honesty gates still hold after the DOM-shot rework).
- **Landing honesty gates (BUILD-11 Build B — MUST hold at every push):** no fabricated logos/testimonials/star-ratings/user-counts/"$X raised"; no advertised feature that isn't live in-product; the product visuals faithfully reflect the real product (as of BUILD-12 they are DOM renders of the real components with clearly-sample-labeled values — not photos, but they must still not misrepresent what ships); the interactive calculator uses honest math with a visible assumption. These are enforced by `landing-funnel-verify.js`. A future session must NOT "add social proof" by faking it — Steward is pre-pilot and the whole brand is that it won't invent traction. Note Supabase backups still don't exist (don't claim them).
- **Landing product shots are DOM/SVG, NOT raster (BUILD-12 — MUST NOT be reverted):** the hero (goal + retention card), the queue, the receipt, the recovery email, and the three how-it-works shots are live HTML/CSS renders of the real components (Landing.jsx's `HeroShot`/`QueueShot`/`ReceiptShot`/`ImportShot`/`ClimbShot` + the pre-existing `.lp-email`), built to match the crisp `RecoveryCalculator` card. Reason: this is a Vite **static** app (no Vercel/Next image optimizer), so prod serves committed bytes verbatim — the retina blur was never CDN re-encoding, it was raster-of-text softness, and only vector text is crisp at every DPR. Crispness is now verified against **prod-served bytes** by `landing-crispness-prod.js`, not against committed files. Do NOT "optimize" these back into `<img>` screenshots; if a value needs updating, edit the component markup (keep values real or clearly sample-labeled).
- /client/src/pages/Landing.jsx — public landing page. **REBUILT WHOLESALE IN BUILD-73 Part 4 (2026-08-31, branch `landing-rebuild`) — everything in the BUILD-07→29 register below is HISTORY, not current behavior.** The page is now: Nav · Hero with the **donor dot field** · Source strip · **Built for orgs like yours** (kept, Jonathan's explicit mid-build instruction) · One year, the same file · Every dot is a person · What it does · Who built this · Closing CTA · Footer. **GONE:** the photo hero + `hero-choir-*` assets and their preload, the `.lp-reveal`/IntersectionObserver machinery, the recovery calculator, the `.lp-frame` browser chrome, the DOM product shots (`HeroShot`/`QueueShot`/`ReceiptShot`/`ImportShot`/`ClimbShot`), and the "Here is what Steward doesn't do" candor section (deleted on Jonathan's explicit instruction, overriding the brief that said keep it verbatim). The founder bio was rewritten to lead with the reader ("You didn't take this job to chase money.") — mission over fundraising, the dad as the reason the product knows what it knows. **THE DOT FIELD is the one piece with machinery: `client/src/lib/donorField.js`** — 199 donors, a HARD-CODED 74-index drift set (never a shuffle: a reshuffling field looks broken, breaks hydration, and makes the year section's claim false), whose **ORDER is load-bearing** — June's 31 are literally the FIRST 31 of December's 74, so the progression NESTS by construction. ONE `<DonorField>` component, four renders (74 hero @20px/12px; 0/31/74 @11px/7px). **Reduced motion is non-negotiable**: the entrance wave + breathing live entirely inside `@media (prefers-reduced-motion: no-preference)` and the base `.df-dot` carries NO opacity of its own — an `opacity:0` base outside that query means an INVISIBLE hero for anyone with the setting on. Only opacity/transform animate (199 elements). **NO PRICING ANYWHERE** — no plan, tier or founding rate; the `/pricing` route still exists but its nav + footer links are gone; every path ends at Start free (`/signup`) or Talk to the founder (the Calendly modal). **The placeholder machinery is GONE (FIX, 2026-09-12)** — `PLACEHOLDERS` + the dashed-outline `Placeholder` component existed to make an unfilled value obvious on a public page, and the last blank it flagged (the © line's legal entity) was filled when Steward Software LLC was registered in Kentucky. The footer now reads `copyrightLine()` from **`shared/legalEntity.js`, the ONE module that holds the entity name, the state and the address** (Terms and Privacy read it too, to define the party each document had left undefined), and the year is COMPUTED. The two landing gates were INVERTED — they used to require the placeholder be VISIBLE and now require the registered name plus the absence of any bracketed placeholder. **`tests/legal-entity.test.js` is the replacement guard and it is repo-wide**: no bracketed placeholder in source (asserted as a FAMILY — the brief's literal `\[[A-Z][A-Z _]+\]` matches `[SCHEMA_HASH]` and every `[ORG]` param array, so it is a multi-word rule plus a vocabulary rule), no second copy of the entity name anywhere, and — **the rule that stays forever — no tax-ID-shaped string outside an enumerated allowlist of eleven synthetic demo EINs. The legal name is public; the EIN is not, and is nowhere in this repo.** A gift receipt names the CHARITY and is asserted NOT to name Steward Software LLC (`audit/FIX-legal-entity-FINDINGS.md` §BLOCKED-1 — Part 2's fourth assertion, as briefed, would have put the vendor on a §170 receipt issued by a nonprofit). **"Fundraising Effectiveness Project, full-year 2025" is load-bearing copy** — FEP rebased in Q1 2026 and now headlines a QUARTERLY figure. Breakpoints 1080/640/400 (1080 chosen by where the 50px serif headlines wrap badly, not a round number); no horizontal scroll 320→1920; tap targets ≥44px. Guards: **`tests/donor-field.test.js` (31)** the pure nesting/determinism properties, **`tests/landing-field.test.js` (37)** the same read back from computed colours in a real browser + reduced motion + no-price/no-outcome-claim + wiring + responsive, and **`tests/landing-reveal.test.js` (7)** rewritten to pin the permanent rule (content visibility must never depend on an animation succeeding) rather than the retired `.lp-reveal`. Captures `docs/landing/`. **The five old prod scripts (`landing-{funnel,hero,crispness-prod,image,motion}-verify.js`) are RETIRED and consolidated into ONE — `scripts/landing-prod-verify.js` (30 assertions, PROD_READONLY).** It carries forward every gate that was worth keeping and was scattered across them: no fabricated social proof / no logo bar, the FEP attribution incl. "full-year 2025", **no competitor cited as the authority** (Bloomerang republishes FEP's number), no "keep 100%" overclaim, CLS ≤ 0.02 on a full mobile scroll + no auto-popup (both were buried in motion-verify), and **measured** text contrast (buried in hero-verify) — plus the rebuilt page's own rules: NO PRICING (the *inverse* of what funnel-verify demanded — it used to REQUIRE a "$149" signal and a body `/pricing` link), no outcome-claim language, the four dot fields nesting, visible placeholders, and reduced-motion visibility. **What did NOT survive, deliberately: the raster-vs-DOM product-shot policing** (crispness/image) — the rebuilt page has no product screenshots, so the subject is gone, not unwatched; **if a raster product shot is ever reintroduced, that guard comes back with it.** NB the hero lede measures **4.53:1** on cream — clears AA by 0.03, copied exactly from the reference per the brief; darken that grey a step if it ever reads too light. Full detail: `audit/BUILD-73-FINDINGS.md` Part 4. — HISTORICAL REGISTER BELOW (superseded): **BUILD-29 (2026-07-30): hero clarity + premium restraint + section order (finishes the landing).** Four changes on top of BUILD-28: **(1) Hero product card.** A single DOM/vector card (`HeroFloatCard` → `.lp-hero-card`, the BUILD-12 goal-thermometer language — label, 22%, gold→terra fill bar, "$5,501 of $25,000") is floated over the empty lower-right of the hero photo with real elevation (shadow + border), so a first-time visitor reads "software FOR nonprofits," not "an arts org." It stays DOM (crispness guards apply), is a signal not a demo (one card), and is **shown only ≥1140px** (positioned to clear the upper-left copy at every width) and **hidden ≤1139px / mobile** so it never crowds the type. The hero **eyebrow is now orientation, not poetry**: "Donor CRM for small nonprofits" (was "For the people who keep the giving going"). Subhead `maxWidth` trimmed to 496 + scrim bumped to `rgba(...,0.60)` so every line stays inside the dark area (AA over the real image, verified at 1280 & 390). **(2) Premium scroll reveals.** `.lp-reveal` sections fade + rise ~16px (`.38s ease-out`) once as they enter view via ONE `IntersectionObserver` (`is-visible`); **opacity/transform only → zero CLS**. `prefers-reduced-motion` (or no IO) reveals everything immediately — the no-motion path is non-negotiable and CSS-`!important`-enforced. **No parallax** (deliberately skipped — restraint > gimmick, protects LCP). **NO auto popups/modals/interstitials on the landing, ever** — capture is via the CTAs + the founding-partner section only (the Calendly scheduler is user-initiated by "Talk to the founder," not an interruption). Section padding widened to `116px`. **(3) Section order — the founder letter now PRECEDES the founding-partner ASK** (the letter earns the ask). The ask moved out of the candor "Where Steward is today" section into its own section AFTER the founder letter ("Founding partners / Be one of the first five." → the ask → a quiet **pricing signal** "From **$149/month** · keep 100% of every gift · See pricing" linking `/pricing` → an understated "Write to me →" QuietBtn). Order: … product-proof → how-it-works → money strip → candor → **founder letter → founding-partner CTA** → close → footer. **(4) The landing has NO mid-page image band** (FIX 2026-07-30, carried from the prior pass): the full-bleed studio-atmosphere band (`.lp-band-img` + `band-studio-*.webp`, Earl Wilcox) was removed — a tight macro read as *texture*, not a place — assets + the `band-studio` prepare-images job deleted (ASSETS.md records the retirement); if a mid-page breath ever returns, use a wide art-studio ROOM shot at ~half height, NOT a macro. New/updated guards: `landing-hero-verify.js` (+card present/DOM/no-collision-at-1280&1024/mobile-dropped) **24/24**, `landing-funnel-verify.js` (+founder-before-ask, pricing present+linked, no band) **30/30**, `landing-motion-verify.js` (reveals fire once, reduced-motion no-op, CLS≤0.02, no auto-overlay) **8/8**; crispness 34/34, image 18/18, brand-glyph/palette/no-emoji green. CLS **0**. Lighthouse ≥93 re-confirmed on prod after deploy (local `vite preview` under-reports; the change removes an image and adds no network assets, so the BUILD-28 prod 94–99 baseline holds). DSF3 screenshots: `docs/build29-2026-07-30/`. — **BUILD-28 (2026-07-30): the landing is now IMAGE-FORWARD / NONPROFIT-NATIVE — the page's top third is photography + words, the DOM product shots are demoted to PROOF below the calculator.** Structure: full-bleed **photo hero** (`.lp-hero-photo`: a responsive WebP `<img.lp-hero-img>` + a **flat rgba scrim** `.lp-hero-scrim` — never a gradient, gradients stay banned — with the eyebrow/headline/subhead/gold-CTAs over the dark quiet upper-left, plus the BUILD-29 `.lp-hero-card`) → **"Built for orgs like yours" verticals band** (`.lp-vert-grid`, 3 cream cards: Arts & culture / Rescue & relief / Faith & community, serif titles) → calculator → **product-proof** section (`.lp-proof`, the relocated `.lp-frame` browser-chrome home screen — still the BUILD-12 live DOM `HeroShot`, do NOT rasterize) → moments → how-it-works → founder → founding-partner CTA → close. **The photography is ILLUSTRATIVE arts/community work, NOT a Steward customer — `aria-hidden`, never captioned as one** (honest-imagery rule). **Rights: all shipped photos are free-tier Unsplash, recorded in `client/public/ASSETS.md` (source + license per image); never a watermarked Unsplash+ comp or an editorial-only file.** All three verticals cards (Arts / Rescue / Faith) carry free-tier Unsplash photos; the graceful on-palette fallback (cream panel + gold rule) remains in code for any future `img: null` slot but is currently unused. **Hero performance (the hero photo is the LCP element):** responsive `srcset`/`sizes=100vw` WebP (`hero-choir-{960,1280,1920,2560}.webp`, mobile LCP 65–98KB), `fetchpriority="high"`, and a **route-scoped preload** in `client/index.html` (an inline `if (location.pathname==="/")` guard injects the hero preload ONLY on `/`, so app/auth routes that share the static shell never fetch it and the BUILD-07 route-split payload win holds). **Lighthouse mobile floor ≥ 93; CLS ≤ 0.02.** Measured on prod (2026-07-30): mobile perf **94–99**, **LCP 1.9–2.7s** (element = the preloaded `hero-choir-1280.webp`, up from BUILD-12's 1.6s DOM-text LCP), **CLS 0**, TBT ≤40ms. **CLS/perf lesson — do NOT regress the font loading:** the brand fonts must load **`display=optional`** AND **non-render-blocking** (JS-injected `<link>` in the Landing render + preconnects in `index.html`). `display=swap` reflows the hero headline when the serif arrives (CLS ~0.03, over the gate); moving the font stylesheet into a render-blocking `<head>` `<link rel=stylesheet>` spikes FCP (~1.6s→2.9s) and drops perf to ~82. Both were measured and rejected. Swapping the hero is a ONE-LINE change: `HERO_SRC` in Landing.jsx + the matching `href`/`imagesrcset` in index.html. Assets are (re)generated by `scripts/build28-prepare-images.js`; **photo filenames deliberately avoid an `lp-` prefix** so they never trip the crispness/image guards. New guard: `scripts/landing-hero-verify.js` (photo+srcset+fetchpriority present, scrim is a flat non-gradient wash, **measured WCAG-AA contrast ≥4.5:1 over the real composited image at 1280 & 390 px**, no fabricated proof in the band) — 18/18; existing `landing-funnel-verify` 24/24, `landing-crispness-prod` 34/34, `landing-image-verify` 18/18, `no-emoji`/`brand-glyph` all still green; DSF3 screenshots in `docs/build28-2026-07-30/`. — **BUILD-12 (2026-07-18): product shots are now LIVE DOM/SVG, not raster screenshots, and the founder letter is Jonathan's real voice.** The hero (goal thermometer + retention card), the queue, the receipt, and the how-it-works trio are React components (`HeroShot`/`QueueShot`/`ReceiptShot`/`ImportShot`/`ClimbShot`, module-level in Landing.jsx) that reproduce the real product components with real / clearly-sample-labeled values — built to match the already-crisp `RecoveryCalculator` card; the recovery-email moment was already DOM (`.lp-email`). All `lp-*.webp` assets, their srcsets, the `GOAL_OVERLAY` constant, the index.html hero preload, and `scripts/landing-capture.js` were removed. The one motion (goal bar filling to its true 22%) is now a real CSS bar (`.lp-goal-fill`, `@keyframes lpFill`, off under reduced-motion) instead of an overlay on a screenshot. The founder band's `TODO-founder-voice` marker is gone: the letter is now Jonathan's own "Why I built Steward" (dad built the donor CRM; the mid-sized-nonprofit gap; the flat-price/0%-fee promise; "I'll fix it this weekend"). Per the no-competitor-names decision, the source draft's named competitors were replaced with "the legacy tools"; the avatar is still a placeholder for a real founder photo. Crispness is verified against **prod-served bytes**: `landing-crispness-prod.js` **34/34 vs prod** + `landing-image-verify.js` **18/18 vs prod** + `landing-funnel-verify.js` **24/24 vs prod**; Lighthouse **mobile 99 on prod** (LCP **1.6s** — DOM text, down from BUILD-11's 2.7s image LCP; CLS 0.011). Committed DPR-3 prod crops: docs/landing-crispness-2026-07-18/ (visibly sharp — the human gate); layout: docs/landing-funnel-2026-07-18/. — Prior register (BUILD-07/BUILD-11) unchanged below: **BUILD-07 book-register rebuild (2026-07-17), extended into an honest funnel with heart + an interactive wedge (BUILD-11 Build B, 2026-07-18)**. BUILD-11 added: (1) **browser-window chrome** around the hero (`.lp-frame`/`.lp-frame-bar` — window dots + `app.stewardapp.dev` URL pill) so the product reads like a real app — the chrome lives OUTSIDE `.lp-shot-wrap`; (2) an **interactive recurring-loss calculator** (`RecoveryCalculator`, `.lp-calc`) — a slider for monthly recurring giving → the annual involuntary-churn loss (`× 12 × 24%`, midpoint of the cited 20–30% range, assumption shown), client-side, honest math not a flattering invented recovery %; terracotta = the leak, green = Steward's job to win it back; (3) a **founder band** with a human avatar slot + "the person who answers your email" line. **`TODO-founder-voice`** marker (in-code, section 6): the founder letter is a DRAFT written FOR Jonathan and must be replaced with his own words (his three open questions — why he built it / who he pictures using it / the promise he'd be embarrassed to break) before shipping as final; the avatar is a placeholder for a real founder photo (never a stock portrait implying a team). Lighthouse **mobile perf 96** (LCP 2.8s throttled, CLS 0.011) against the local preview of the BUILD-11 build — the additions (calculator ~1KB JS, CSS chrome) added no new assets, so the prod-CDN 96 baseline holds; re-confirm against prod after deploy. Verification: `docs/landing-funnel-2026-07-18/` (desktop-full, hero-3x with chrome, calculator-3x, mobile-full) + `landing-funnel-verify.js` 24/24 + `landing-image-verify.js` 12/12 (crispness invariant still holds — chrome didn't degrade the hero). — Original BUILD-07 description follows: **rebuilt wholesale (BUILD-07, 2026-07-17)** in a warm-serious book register: serif hero ("Donors don't leave. They drift. Steward notices."), three-true-numbers subhead (0% platform fees / flat price / 20–30% involuntary recurring churn), three alternating "product moment" sections (real captures: live home queue, the verbatim DEFAULT_DUNNING_BODY email with sample values, the real /receipts/preview PDF), dark money strip ("Your donors give to you. Only you."), the kept-and-refreshed "Where Steward Is Today" candor section with a founding-partner mailto ask, a founder letter (draft flagged in-code for Jonathan's edit), and a real-links-only footer. Honesty commitments preserved: no testimonials, no invented numbers, every claim checked against this file. **`.lp` must carry its own cream background** — index.html's inline `<body style="background:#030712">` beats any stylesheet. The one motion: a CSS overlay on the hero capture's goal bar (measured geometry, fills to its true 22%, ~6s loop, removed under prefers-reduced-motion). CTAs: Start free → /signup; "Talk to the founder" → real Calendly modal (calendly.com/xjca2006) with the founder email visible. **Images (BUILD-08 Phase A, recaptured LOSSLESS + 3x in BUILD-10 Part 2, 2026-07-17)** by the committed `scripts/landing-capture.js` (see its bullet above): six images (lp-home, lp-queue, lp-receipt + How-it-works steps lp-import, lp-attention, lp-climb), each **1x + `-2x` + `-3x` LOSSLESS WebP**, tight element crops displayed near native text size via explicit width/height + a full 1x/2x/3x srcset; the receipt is trimmed to its content (no more empty white bottom half); GOAL_OVERLAY re-measured from the manifest. The q92-lossy / 2x-max BUILD-08 assets read grainy on retina — see the landing-capture.js bullet for the diagnosis + fix; the invariant is now enforced by `scripts/landing-image-verify.js`. New numbered **"How it works" section** (`.lp-hiw-grid`, 3 steps: import → see who needs attention → watch the numbers climb) sits between the product moments and the money strip. Lighthouse **mobile perf 96 against prod CDN** (warm; LCP 2.7s — a cold-CDN first run measured 91, still ≥90; the LCP element loads the 174KB 2x hero, not the 630KB 3x). Verification: docs/landing-retina-2026-07-17/ (DPR-3 hero/queue/receipt crops — crisp text) + docs/landing-2026-07-17-phaseA/ (layout screenshots).
- /client/src/pages/LoginPage.jsx — login, writes localStorage directly; post-login redirect is `/dashboard` (was `/today`)
- /client/src/pages/SignupPage.jsx — signup
- /client/src/pages/WelcomePage.jsx — 5-step onboarding that finishes with real data, not a blank slate (org basics → import donors → set first goal → first impact metric → finish). See "Onboarding flow" below.
- /client/src/pages/GivePage.jsx — public donation page at /give/:orgSlug
- /client/src/api.js — apiFetch, streamAI, adaptData helpers
- /server.js — Express backend (all routes)
- /auth.js — auth middleware (requireAuth, requireAdmin)
- /db.js — Supabase client

## Key files
- client/src/components/Donors.jsx — donor list + ALL importers; `DonorImport` is now exported (was module-private) so WelcomePage's onboarding flow can reuse it directly as the import step's centerpiece
- client/src/components/Finance.jsx — **reintegrated + redesigned (BUILD-09, 2026-07-17)**; visible in nav again (see "Finance (reintegrated)" below)
- client/src/components/Settings.jsx — includes an Impact Metrics admin panel (create/edit/delete), mirroring the existing Custom Fields UI pattern
- client/src/components/UpgradeModal.jsx
- server.js
- db.js

## Active tabs (App.jsx TABS array — current, post-pivot)
dashboard ("Home") → donors → **pipeline** → **fundraising** → grants → communications → **tasks** → **workflows** → reports → finance → settings
*(`portal` is defined in `TABS` but HIDDEN from the CRM's navigation — see the Donor Portal note below.)*

**Desktop sidebar is GROUPED (BUILD-20 Part 3, 2026-07-18).** The flat ~10-item rail now reads as labeled sections (subtle uppercase labels on the BUILD-12 palette): **Home** (ungrouped, top) · **People** (Donors · **Pipeline** · Tasks) · **Fundraising** (Fundraising · Grants · Communications · Workflows) · **Insight** (Reports · Finance) · **Settings** (pinned bottom, unchanged). Config = `NAV_GROUPS` in App.jsx; the sidebar looks each id up in `TABS` (so `TABS` order/lookups elsewhere are untouched). **Pipeline stays a TOP-LEVEL item within People — NOT nested under Donors.** Mobile (`BOTTOM_TABS`/`MORE_TABS`) is unchanged (no grouping). Guarded by `tests/locked-features.test.js`.

**Team-gated surfaces are VISIBLE-BUT-LOCKED, Givebutter-style (BUILD-20 Part 4, 2026-07-18) — one reusable `LockedFeature` wrapper.** Instead of hiding a `[Team]` surface (or showing a bare 403/upgrade card), a Core user sees the REAL surface rendered with the org's **own data behind frosted glass** + an "Unlock with Team — See plans →" overlay. `LockedFeature`/`LockGlyph` live in `shared.jsx` (`LockGlyph` is a monochrome SVG padlock — NOT an emoji, per Part 1). The wrapper dims children (`blur` + `opacity` + `pointerEvents:none`) so the preview is genuinely non-interactive. **Sidebar:** Team-gated items (`TEAM_GATED=new Set(["pipeline"])`) show a small lock glyph for Core users (client tier via `planTier` from `/billing/status`: growth/impact/trialing = team, else core) — visible, not removed. **Surfaces using it:** (1) **Pipeline.jsx** — the board is built once as `const board` and, when `data.locked`, wrapped in `LockedFeature` (the server now returns the Core org's real columns with `locked:true`, see below); (2) **Reports.jsx** — the Solicitations (and monthly per-officer digest) sub-tabs dim the real report body behind the glass when `d.locked`. **GUARDRAIL (do not reopen BUILD-19): reads are visible, WRITES stay server-gated.** `POST /pipeline/:donorId/move` + `/donors/:id/opportunities` are still `requirePlan('team')` → 403; `GET /pipeline` now computes the board for Core too (`const locked = tier!=='team'`, all org-scoped reads a Core user already has) and returns `locked` — it does NOT open a write path. `GET /reports/:key` returns team-only reports to Core with `data.locked=true` (a report is a pure read) BUT the **CSV export of a locked report stays 403** (look, don't pull the artifact out). Guarded by `tests/locked-features.test.js` (source: wrapper + wiring + writes-stay-gated) + live tier gating in `tests/moves.test.js` (Core board `locked:true` with own columns; writes 403/402) + `tests/reports-cadence.test.js` (Core solicitations 200+locked, CSV 403).

**Donor-profile Core/Team split (FIX, 2026-07-19) — the CRM core is Core; only the major-gifts LAYER is Team, and it uses the SAME `LockedFeature` wrapper.** The Pipeline tab was Team-gated but the same major-gifts capabilities sat ungated on the **donor profile** one screen over — a gating hole where a Core (seed) org got the Team features free. Fixed by gating those panels consistently with the Pipeline tab, **without removing the donor profile from Core** (it's core CRM). The split: **Core keeps** (never gated) identity/contact, lifetime/last-gift, giving-history chart, the Gifts & Pledges / Funds / Related / Materials / Activity tabs, tasks/follow-ups, households, planned-giving & designations, tags, notes, and the relationship-owner **display** (read-only). **Team-locked for Core** (real panel behind frosted glass + "Unlock with Team" CTA, via a `lockMajor(children,opts)` helper in `DonorProfile` that reuses the ONE shared `LockedFeature` — plain function, not a `<Component>`, so Team never remounts): the **Pipeline — moves & asks** panel, the consolidated **major-gift rail** (Suggested Move + **Move Stage** + **Wealth Score** + Suggested Actions), the **Sequences** enroll panel, and the **Reassign** control (owner shown read-only; the Reassign button is hidden for Core). The Directory's same controls are hidden for Core too (bulk "Move to stage", bulk "Assign to", per-row "Assign" gate on `teamPortfolios`). **Server-enforced, not just hidden** (`requirePlan('team')` → 403 for Core): `PATCH /donors/:id/stage`, `PATCH /donors/bulk-stage`, `PATCH /donors/:id/assign`, `PATCH /donors/bulk-assign`, `POST /donors/:id/score` (wealth), `POST /sequences/:id/enroll` (plus the already-gated `POST /pipeline/:donorId/move`, `POST /donors/:id/opportunities`, `PUT /opportunities/:id`). **Reads stay visible** for Core so the previews show its own data behind glass: `GET /donors/:id/moves`, `/opportunities`, `/move-suggestions` are un-gated. **This REVERSES the earlier "per-donor stage dropdown is Core-fine" note (BUILD-19/22)** — managed stage changes are now Team; Core sees stage as a read-only label. Client tier probe on the profile is `GET /portfolio/officers` (`isTeam=tier==='team'`). Guarded by `tests/locked-features.test.js` (source: `lockMajor` reuse, panels wrapped, reassign gated, CRM core NOT gated, every write route `requirePlan('team')`, reads open) + live gating in `tests/moves.test.js` (Core→403 / Team→200 on all six routes; Core reads 200). DSF3 screenshots: `docs/donor-profile-gating-2026-07-19/` (core-donor-profile = CRM core visible + major-gifts locked; team-donor-profile = full) via `scripts/donor-profile-gating-capture.js`.

Pipeline was **added 2026-07-18 (BUILD-15)** — the moves-management / major-gifts prospect board (see "Moves management & prospect pipeline" below). It sits between Donors and Fundraising; on mobile it's the first item in the "More" drawer (`MORE_TABS`). It is `[Team]`-gated: a Core org sees a graceful upgrade state, not a broken tab.

**Pipeline is a SINGLE canonical surface with ONE gate (BUILD-19, 2026-07-18).** The top-level Pipeline tab (`Pipeline.jsx`) is the ONLY pipeline board. On Core, `GET /pipeline` returns `locked:true` and the client renders the locked state (BUILD-19 shipped a plain upgrade card here; **BUILD-20 Part 4 replaced it with the `LockedFeature` preview — the org's real board behind glass + an unlock CTA — and made the server return the Core org's own columns; WRITES stay `requirePlan('team')`-gated**, see "Team-gated surfaces are VISIBLE-BUT-LOCKED" above). BUILD-15 had left a **second, ungated** board behind — the pre-existing Donors → "My Pipeline" kanban (`DonorKanban`, client-side over `donors.stage`) — so a Core org saw the pipeline locked in one place and wide open in another. BUILD-19 **retired the Donors→My Pipeline backdoor entirely** (decision: pipeline is **purely Team**; Core users get a lightweight "my donors by stage" via the Directory's stage filter). Home's portfolio/pipeline command cards now deep-link to the canonical Pipeline tab (`onNavigate("pipeline")`), not the old donors sub-view. The per-donor `DonorProfile` **Move Stage** control (`moveToStage`, `PATCH /donors/:id/stage`) is **now Team-gated too** (donor-profile Core/Team split FIX, 2026-07-19 — this reverses BUILD-19's earlier "Core-fine single-donor label change" call; managed stage changes are a major-gifts action, Core sees stage read-only). Guarded by `tests/pipeline-gating.test.js` (source-level: no My Pipeline tab / no `DonorKanban` / one gated surface); live tier gating (Core→locked / Team→board / read_only→402) stays covered by `tests/moves.test.js`.

Fundraising was **added 2026-07-18 (BUILD-11)** — see "Fundraising (BUILD-11)" below. It sits between Pipeline and Grants; on mobile it's in the "More" drawer (`MORE_TABS`).

Tasks was **resurrected 2026-07-18 (BUILD-13 Part 1)** — un-hidden AND rebuilt from the deprecated local-state stub into a real API-driven daily-driver surface (see "Tasks (BUILD-13)" below). It sits between Communications and the new Workflows tab. **Workflows** was **added 2026-07-18 (BUILD-13 Part 3)** — the retention-recipes automation tab (see "Workflows engine" below), between Tasks and Reports. Events/Volunteers/Board remain commented out of `TABS` with a `// DEPRIORITIZED` marker — re-enable by uncommenting, nothing else to restore.

Finance was **reintegrated 2026-07-17 (BUILD-09)** — un-hidden AND rebuilt to current design standards (see "Finance (reintegrated)" below). (Analytics was deleted outright — see Strategic pivot; the Reports tab added 2026-07-16 is its deliberate replacement — see "Reports" below.)

**Donor Portal is HIDDEN FROM THE CRM's navigation (2026-09-10)** — the Events/Volunteers/Board move, one level up. `CRM_HIDDEN_TABS=new Set(["portal"])` in App.jsx and `tabAllowed=id=>isPortalTier?PORTAL_TIER_TABS.has(id):!CRM_HIDDEN_TABS.has(id)` is the ONE place the two rules meet (desktop sidebar, mobile More drawer and `navigateTo` all read it; a CRM org asked to go to a hidden tab lands on Home instead). **Nothing is deleted**: `DonorPortalHub.jsx`, `/portal-editor`, every `/portal*` route, `portal_settings`/`portal_pages`/`portal_assets`, and the portal/portal-page/portal-visual/portal-designation/portal-crop/theme-assets suites are all untouched — re-enable by removing one id from one Set. **A `plan="portal"` org KEEPS the tab** (its entire product is that surface; the id therefore stays in `TABS`/`NAV_GROUPS`/`MORE_TABS`, which are the lookup source for both tiers). Settings' **Donor Portal** section was only a POINTER to the tab, so it is `portalTierOnly:true` now and filtered out of `SETTINGS_TABS` for CRM orgs. **GIVING PAGES IS UNAFFECTED** — a different surface, still Settings → Giving Pages, along with Stripe Connect, donor-covers-fees, the org timezone and the public `/give` page. Pinned by `tests/locked-features.test.js`; verified in a browser at the time (CRM sidebar has no Donor Portal item; a portal-tier org's does).

Mobile bottom bar (`BOTTOM_TABS`): dashboard ("Home"), donors, grants, settings
Mobile "More" drawer (`MORE_TABS`): pipeline, fundraising, communications, tasks, workflows, reports, finance (Events/Volunteers/Board are commented out here too)

## Component files (client/src/components/)
- shared.jsx — T (design tokens, now includes `bgDeep` and `terracotta`), fmt, fmtFull, daysDiff, daysUntil, SC, askClaude, buildContext, STAGES, STAGE_THRESH, STAGE_ACTION, TIER_COLOR, donorScore, retentionRisk, moveUrgency, GlobalStyles, Spin, Pill, Card, SectionLabel, AIBtn (default label "Suggest", not "AI Assist"), AIPanel (badge reads "Suggested", not "AI Intelligence"), MetricCard, EmptyState, PageTitle, GivingHistoryChart, TpField, TpYesNo, TouchpointTimeline, VoiceMemoModal (built but currently unused — see "Voice memo capture (shelved)" below)
- Dashboard.jsx — exports `Dashboard` only. **`AIChat` (the global "Ask AI" chat overlay) was removed entirely**, not hidden — see Strategic pivot. `Dashboard` is now the action-queue-first home screen (goal banner, "Needs Your Attention" queue, pipeline funnel, next-grant-deadline tile, Stewardship Debt/First-Touch Delay headline metrics, recurring-gift-at-risk card) — the old 4-card KPI stat row, Quick Actions sidebar, and Recent Activity feed are gone, not just restyled. See "What's built" below for the full description. **(2026-07-15)** Greeting ("Good evening, {name}") lives on the page's own cream background between the nav and the goal card, not inside the dark card. **Greeting windows are `client/src/lib/greeting.js` (`greetingForHour`, JSX-free/Node-testable, ONE source of truth — BUILD-36 B3): evening 5 PM–4 AM · morning 4 AM–noon · afternoon noon–5 PM.** A bare `hour<12` used to say "Good morning" at 12:23 AM; guarded by `tests/greeting.test.js`. Every color on this page is locked to the five-color palette (see "Design system" above) — no exceptions, including hero-metric status coloring. The goal card has an admin-gated pencil/edit control next to the label that reopens the same set-goal modal pre-filled with current values — editing works by submitting a new overlapping `POST /goals` row (there's no `PUT /goals/:id`; `GET /goals/active` always resolves to the most-recently-created row whose period contains today, so a new submission supersedes the old one by design, not a special-cased edit path). Hero-card copy is narrative-first (a sentence of context with the number as supporting evidence), not a bare numeral with a caption. **(2026-07-16)** The metric hierarchy (Retention Rate hero → First-Touch Delay secondary → Stewardship Debt demoted strip) is a deliberate, founder-driven decision (`436a6b5` "demote, don't delete" after live "not a fan" feedback on the debt hero; `d72dbd0` after "wall of cold statistics" feedback) — an older "four equally-weighted drillable KPI cards" ask was investigated (BUILD-04 Strike 3) and resolved as superseded by that feedback; do not resurrect equal-weight stat cards here without new direction.
- Donors.jsx — exports Donors, and now also exports `DonorImport` (reused directly by WelcomePage's onboarding flow) (includes FollowUpTaskModal, LogTouchpointModal, EditDonorModal, DonorProfile, ReEngageView internally). **The Donors → "My Pipeline" kanban was retired in BUILD-19** (the `DonorKanban` component and its `view==="pipeline"` sub-tab are gone) — it was a free, ungated pipeline board that contradicted the Team-gated Pipeline tab; see "Pipeline (single canonical surface)" below. The per-donor Move Stage control on `DonorProfile` (`moveToStage` → `PATCH /donors/:id/stage`) is **Team-gated** (donor-profile Core/Team split FIX, 2026-07-19 — Core sees stage read-only behind the locked major-gift rail; reverses the earlier "Core-fine" note). `DonorProfile`'s Gifts & Pledges tab shows a recurring-gift status chip (Active/Payment failed/Recovering/Recovered/Canceled) + "Send card-update link" button when at-risk — see "Recurring gift recovery" under Database
- Events.jsx — exports Events (EventCard, EventDetail, NewEventPanel, FollowUpModal internally) — **hidden from nav**, code/routes/tables intact
- Grants.jsx — exports Grants (includes GrantProfile, FindGrants internally). GrantProfile now accepts an `initialGrantId` prop for deep-linking (used by the home screen's next-grant-deadline tile)
- Communications.jsx — exports Communications (email campaigns, templates, audience segmentation, Resend API, open tracking, Sequences, Milestone Drafts review queue). Accepts `initialNav`/`highlightDraftId` props for deep-linking from the home screen's queue. See "Retention & stewardship" below.
- Reports.jsx — exports Reports (six fixed reports: horizontal top tabs via `SectionTabs` with the active report's "question this answers" as a subtitle line (was a left rail pre-2026-07-17), param bar, narrative summary line, sortable dense tables, CSV download — see "Reports" section below). Takes `onNavigate`; LYBUNT/SYBUNT/Top-Donors rows deep-link to the donor profile via `onNavigate("donors",{selectDonorId})`.
- Volunteers.jsx — exports Volunteers — **hidden from nav**, code/routes/tables intact
- Board.jsx — exports Board — **hidden from nav**, code/routes/tables intact
- Finance.jsx — exports Finance (SectionTabs: Overview · Transactions · Funds · Budgets · Accounts · Audit Log; includes fund sparklines, Money-in Stripe strip, unified ledger with source badges) — **reintegrated + redesigned to current standards (BUILD-09, 2026-07-17)**; takes `isReadOnly` + `onNavigate`. See "Finance (reintegrated)" below
- Fundraising.jsx — exports Fundraising (SectionTabs: Overview · Campaigns · Giving Pages · Funds); takes `data` + `isReadOnly` + `onNavigate`. **New 2026-07-18 (BUILD-11)**. The money-moving home: live goal thermometer + pace on Overview, campaign cards with live thermometers, giving-page surfacing (QR/embed, links to Settings for CRUD), Funds cross-link to Finance. Every figure is a live SUM(gifts), never a stored counter. See "Fundraising (BUILD-11)" below
- Tasks.jsx — exports Tasks — **hidden from nav**, code/routes/tables intact
- Settings.jsx — exports Settings (Stripe connect, QR code, donation widget embed, team management, invite modal, Custom Fields manager, Impact Metrics manager — same UI pattern as Custom Fields). As of 2026-07-17 the eight former SecHead groups are `SectionTabs` sections (only the active one renders); takes `initialSection` for deep-linking (see App.jsx notes). The Gmail card lives under Integrations (it had drifted under Giving Pages).
- TopBar.jsx — exports TopBar(auth, logout, onNavigate); the BUILD-08 global top bar (search + ⌘K, help menu, user chip/sign-out) — full behavior documented in the App.jsx bullet under "Project structure"
- PlanPicker.jsx — exports default PlanPicker(open, onClose) modal; renders **`CHECKOUT_PLANS`** (the Stripe-wired **Core/Team** set, BUILD-24) from pages/Pricing.jsx; selecting a plan calls POST /billing/create-checkout (maps `core`→`STRIPE_PRICE_CORE`, `team`→`STRIPE_PRICE_TEAM`). Used by App.jsx banner "Reactivate"/"Choose a plan" buttons — NOT the Stripe Customer Portal (see SaaS billing section). **BUILD-24 cutover (2026-07-19):** PlanPicker was repointed from the legacy `BILLING_PLANS` (seed/growth/impact) to `CHECKOUT_PLANS` (Core/Team). Checkout goes live the moment `STRIPE_PRICE_CORE`/`STRIPE_PRICE_TEAM` are set; if unset, create-checkout returns a clean `plan_not_configured` message (no 500). `BILLING_PLANS` is retained in Pricing.jsx for reference/legacy reactivation only — do NOT put it back in the modal. The founding-partner price is deliberately absent from both the modal and the public page (super-admin-only, see "Platform billing cutover" below).
- **Public pricing model (2026-07-18) — Core $149 / Team $299 + Foundation add-on.** `pages/Pricing.jsx` was repackaged from the 3-tier Seed/Growth/Impact display to a **2-plan commercial model** split on Brad's line ("do you have gift officers to manage?" — no → **Core** is the whole product; yes → **Team** adds the major-gifts machine). Core (≤5,000 active donors · 3 users): full CRM + 0%-fee giving, receipts/year-end, households/soft-credit, typed roll-up goals, retention workflows (failed-card recovery + instant gift→thank), reports incl. 3-year/annual, Week-in-Review, org branding. Team (≤25,000 active · 10 users): moves management, officer portfolios, per-officer monthly + solicitations reports, multi-officer digests, full white-label. Plus an opt-in **Foundation Portal** add-on (grant-lifecycle mgmt; price TBD → "Ask about the add-on" CTA, no price shown). **This was a PUBLIC-SURFACE-ONLY pass** (deliberate scope, user-confirmed): no Stripe rewire, no plan-enum change, no donor-band/active-donor enforcement — the backend still runs seed/growth/impact via `orgPlanTier`/`PLAN_LIMITS` (all-records counting). The page's CTAs route to the free trial (`/signup`) / founder contact, never a live checkout against non-existent Prices. **Honesty guard (same rule as Landing):** every listed feature is live EXCEPT Team's "custom domain / remove Steward branding" (white-label custom-domain is a deferred tier per BUILD-13) — it renders with a "Coming soon" chip rather than a bare live claim. **Early-partner pricing stays off-menu** (never rendered on the page/app/admin). The `active donors, not every record` band language is forward commercial positioning (not yet enforced). Screenshots: docs/pricing-2026-07-18/ (desktop + mobile). A full billing cutover (create $149/$299 Stripe Prices, migrate orgs, implement active-donor counting + hard bands) is a separate, deliberate backend task — NOT done here.

## Routing (IMPORTANT)
- / → Landing (public)
- /login → LoginPage (public)
- /signup → SignupPage (public)
- /welcome → WelcomePage (auth required)
- /dashboard → App/AppShell (auth + onboarded required)
- /give/:orgSlug, /give/:orgSlug/:pageSlug, /give/:orgSlug/:pageSlug/:fundraiserSlug → Donate.jsx (public — org-wide page, Giving Page, and peer-to-peer fundraiser page, one component, not three forks; see "Giving Pages"/"Peer-to-peer fundraising")
- /fundraiser/manage/:token → ManageFundraiser.jsx (public, token-authenticated — a peer fundraiser's own "edit my page" screen, no login)
- /admin → AdminDashboard (super admin only — RequireSuperAdmin guard checks localStorage npe_user.isSuperAdmin)
- App.jsx renders <AppShell /> directly — NO internal router

## The actor on every write (moved from the old CRITICAL WORKING RULES)

- **The actor on every write (BUILD-75 C.1):** every INSERT into an actor table (gifts, donors, pledges, tasks, campaigns, grants, events, households, opportunities, receipts, giving_pages, planned_gifts, volunteers, board_members, fin_transactions, sequences) records `created_by`/`created_by_name` — a user id for a human, a system identity string ("system:stripe-webhook", "system:workflow:<recipe>", "system:auto") for a non-human path, never null on a new write. `tests/actor-stamp.test.js` fails the build on an unstamped, unclassified insert. NULL means "predates BUILD-75" — never backfill guesses.

## Admin data integrity
Diagnostic tooling added after manually deleting test-user rows in Supabase's Table Editor left open questions about orphaned data (see "Current priorities" — the actual investigation is still outstanding, this section documents the tooling only).
- `GET /admin/data-integrity` (requireAuth + requireSuperAdmin) — reports orgs with no matching `users` row, plus dangling foreign-key references to deleted user IDs, without changing anything.
  - `DANGLING_USER_REF_CHECKS` — columns that reference a user but aren't unique/required, checked for stale IDs: `interactions.created_by`, `donors.assigned_to`, `donor_materials.uploaded_by`, `milestone_drafts.reviewed_by`, `note_reminders.sent_by`, `board_reports.generated_by`, `fin_audit_log.user_id`, `ai_log.user_id`, `invites.invited_by`.
  - `DANGLING_USER_ROW_CHECKS` — whole rows keyed to a now-missing user: `gmail_connections.user_id` (org-scoped), `password_reset_tokens.user_id` (not org-scoped).
- `POST /admin/data-integrity/fix` (requireAuth + requireSuperAdmin) — nulls the dangling FK references found above; does NOT delete orgs or donor/gift data as a side effect (orphaned-org cleanup is a separate, deliberate action — see below).
- **`DELETE /admin/orgs/:id` cascade fixed** — was missing `milestone_drafts`, `note_reminders`, `donor_materials`, `planned_gifts` (all ordered BEFORE the `donors` delete, since they carry `donor_id` FKs), plus `board_reports`, `fundraising_goals`, `impact_metrics`, `metric_snapshots`, `email_suppressions` (org_id only, no ordering constraint). Use this route (not manual table deletes) to remove a throwaway/test org — it deletes everything scoped to the org in FK-safe order.

## Database — key tables and columns

## Key patterns
- TpField and TpYesNo MUST stay at module level (not inside components) — defined in shared.jsx. Moving them inside a component causes React to remount inputs on every keystroke.
- Donor wealth score: POST /donors/:id/wealth-score → calcWealthScore() uses 5 components (giving history, recency, frequency, capacity signals, engagement). DB columns: wealth_score, capacity_tier, score_confidence, score_last_updated, score_rationale. TIER_COLOR maps tier → hex color.
- Finance sync: every gift saved via POST /donors/:id/gifts also inserts a row in fin_transactions with donor_id set — one-way sync, no double-write on edit.
- Sample data flag: `is_sample BOOLEAN DEFAULT false` column exists on donors, gifts, grants, events, event_attendees, campaigns, interactions, tasks, fin_transactions, fin_funds, volunteers, board_members. Clear route deletes in FK-safe order (children before parents). Sample funds use hardcoded IDs: `fund_smpl_general`, `fund_smpl_edu`, `fund_smpl_capital`.
- STAGES in shared.jsx is an object array (with color/label). Communications.jsx uses a plain string array called STAGE_LIST — kept separate to avoid shadowing. **(2026-07-15)** Stage colors are locked to the five-color palette (see "Design system"): prospect=bgElevated, qualify=gold, cultivate=greenMid, solicit=green(accent), steward=greenDk, lapsed=terracotta — deliberately alternating green shades with gold/terracotta rather than four greens in a row, so adjacent pipeline stages stay visually distinct. prospect specifically avoids `T.ink` (a dark column-header hex), which would make prospect's left-border accent invisible against a same-color column background. This cascades automatically to the Pipeline Funnel (Home) and the CSV-import stage-assignment preview (`Donors.jsx`'s `STAGE_COLORS`, now derived from `STAGES` instead of a separate hand-maintained palette). (The Donors → My Pipeline `DonorKanban`, which this note originally also referenced, was retired in BUILD-19 — see "Pipeline is a SINGLE canonical surface".) (It previously also cascaded to Analytics' Pipeline Velocity chart — Analytics deleted 2026-07-16.)
- All AI features stream through /ai/stream on backend via askClaude (= streamAI from api.js).
- Stripe Connect: POST /stripe/connect returns a Stripe account link URL; /stripe/webhook handles payment_intent.succeeded and checkout.session.completed.
- Activity log templates: LogTouchpointModal in Donors.jsx has per-type prompt templates. On save, if type is Call/Meeting, a follow-up task is silently created and a toast shown.
- Mobile: GlobalStyles() in shared.jsx is the single CSS home for all @media(max-width:768px) rules. Use className + !important to override inline JSX styles.

## Custom Fields
- `custom_fields` table: id, org_id, label, field_type (text/number/date/dropdown/checkbox), options (JSONB), required (boolean), field_order, show_in_directory (boolean, default false), created_at
- `custom_field_values` table: id, org_id, donor_id, field_id (FK→custom_fields ON DELETE CASCADE), value (TEXT), updated_at. UNIQUE(donor_id, field_id)
- `GET /custom-fields` — list org fields ordered by field_order
- `POST /custom-fields` — create field (admin only)
- `PUT /custom-fields/reorder` — reorder fields by passing `ids` array (MUST be declared before `PUT /custom-fields/:id`)
- `PUT /custom-fields/:id` — update field (admin only)
- `DELETE /custom-fields/:id` — deletes values then field (admin only)
- `GET /donors/custom-field-values/all` — returns all custom_field_values for the org as `[{donorId, fieldId, value}]`. Declared BEFORE `GET /donors/:id` to avoid Express collision.
- `GET /donors/:id/custom-fields` — returns fields + values joined (LEFT JOIN) for a donor
- `POST /donors/:id/custom-fields` — upsert a value for a field on a donor (ON CONFLICT DO UPDATE)
- Settings.jsx: Custom Fields section between Team Members and Account Actions. Field manager with add/edit/delete. Dropdown type shows option builder.
- DonorProfile (Donors.jsx): Custom Fields section shown only when org has custom fields (cfData.length > 0). Inline edit per field with appropriate input type.

## Org_id scoping (security)
- All donor/grant/task/volunteer/board endpoints use AND org_id = ? on SELECT, UPDATE, DELETE
- calcWealthScore scopes gifts and interactions queries by both donor_id AND org_id
- Stripe webhook looks up org by stripe_account_id (per-org), then inserts scoped records
